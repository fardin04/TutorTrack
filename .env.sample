# TutorTrack Environment Variables Sample
# Copy this file to .env and fill in your actual values

# ==========================================
# APPWRITE CONFIGURATION
# ==========================================

# Appwrite server URL
# For Appwrite Cloud, use: https://cloud.appwrite.io/v1
# For self-hosted, use your server URL
VITE_APPWRITE_URL=https://cloud.appwrite.io/v1

# Your Appwrite project ID
# Get this from your Appwrite Console -> Settings -> Project ID
VITE_APPWRITE_PROJECT_ID=your_project_id_here

# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# Your Appwrite database ID
# Create a database in Appwrite Console and copy the ID here
VITE_APPWRITE_DATABASE_ID=your_database_id_here

# ==========================================
# COLLECTION IDS
# ==========================================

# Students collection ID
# Collection to store student information
# Required attributes: name (string), daysPerWeek (integer), blockedDays (string), tutorId (string)
VITE_APPWRITE_COLLECTION_STUDENT_ID=your_student_collection_id_here

# Tutor availability collection ID  
# Collection to store tutor availability and preferences
# Required attributes: workingDays (string), maxStudents (string), tutorId (string)
VITE_APPWRITE_COLLECTION_AVAILABILITY_ID=your_availability_collection_id_here

# Class logs collection ID
# Collection to store class completion/attendance logs
# Required attributes: studentId (string), date (string), status (string), tutorId (string)
VITE_APPWRITE_COLLECTION_LOGS_ID=your_logs_collection_id_here

# ==========================================
# SETUP INSTRUCTIONS
# ==========================================

# 1. Create an Appwrite account at https://cloud.appwrite.io
# 2. Create a new project
# 3. Create a database within your project
# 4. Create the three collections mentioned above with their required attributes
# 5. Enable Email/Password authentication in Auth settings
# 6. Copy your project details and paste them above
# 7. Rename this file to .env
# 8. Never commit your .env file to version control!

# ==========================================
# COLLECTION SCHEMA REFERENCE
# ==========================================

# Students Collection:
# - name: String (required) - Student's full name
# - daysPerWeek: Integer (required) - Number of classes per week
# - blockedDays: String (required) - JSON array of unavailable days
# - tutorId: String (required) - ID of the tutor who owns this student

# Availability Collection:
# - workingDays: String (required) - JSON array of available days
# - maxStudents: String (required) - JSON object with max students per day
# - tutorId: String (required) - ID of the tutor

# Logs Collection:
# - studentId: String (required) - Reference to student
# - date: String (required) - Date of the class (YYYY-MM-DD)
# - status: String (required) - 'completed' or 'missed'
# - tutorId: String (required) - ID of the tutor
